# JIS X 0208 罫線素片配置プロジェクト

## 完了した作業

### Phase 1: JIS X 0208 8×4 全探索（完了）

**目標**  
JIS X 0208 の罫線素片 32 字を盤面 8×4 に一回ずつ置き、すべての接続が一致（余りが出ない）する配置を全列挙する。

**実装内容**

1. **タイル表現と制約**
   - 各罫線素片は**固定向き**（回転できない）
   - 各辺は「接続の有無」と「接続の種類」を表す  
     sides = [N, E, S, W], 値 0/1/2
     - 0: 接続なし
     - 1: 細線接続
     - 2: 太線接続
   - jis32.json: JIS X 0208 の罫線 32 字を完全に定義

2. **バックトラッキング探索**
   - 隣接検査: 隣接辺の値が完全に一致する（0-0, 1-1, 2-2 いずれか）
   - 境界検査: 盤端のすべての辺は 0（未結合）
   - 盤面全体をタイルで埋めるまで探索

3. **正規化（canonicalization）と重複排除**
   - **重要な設計決定**: タイルは固定向き、盤面は D4 群（8 変換）の対象
   - 盤面 B に対して 8 個の D4 変換を試みる:
     1. 盤面全体を回転・反射
     2. 各セルの sides も同じ変換を適用
     3. **検証**: 変換後の sides が実際に存在するタイル（jis32.json に定義）に対応するかチェック
     4. 1 つでも対応しなければその変換は無効
   - 有効な変換に対して、さらに型ラベル置換（1↔2）を施す
   - すべての有効な変換済みサインを生成し、辞書順で最小のものを代表とする

4. **接続性の分類**
   - 「接続成立」エッジ: 隣接辺の値が 0 でなく、かつ一致する
   - BFS で全 32 タイルが 1 つの連結成分を成すなら「単結合」
   - 2 つ以上の分離した連結成分があれば「非単結合」

**結果**
- **全 37 代表解** が見つかった
  - 単結合解: 25 個
  - 非単結合解: 12 個（有効だが構造が異なる）
- 探索ノード数: 約 2,180 万
- 実行時間: 数分程度

**出力**
- `solution_1.json` ～ `solution_37.json`: 各解の盤面（JSON 形式、32 要素の配列）
- `gallery.html`: インタラクティブ HTML ギャラリー（単結合/非単結合で区分）
- `solutions_summary.txt`: 全解の ASCII アート表記

## 重要な設計決定

### 誤解の再発を防ぐために

**🔴 よくある誤り**: タイルを個別に回転させて配置する

**✅ 正しい理解**: 
- タイルは固定向き（U+250C の ┌ は常に ┌）
- 盤面全体を回転・反射させたとき、回転後の各セルに対して、その回転済み sides を持つタイルが存在するかを確認する
- 存在しなければ、その盤面変換は**無効**（その盤面は解候補にならない）
- 例: タイル U+250C (┌) の sides が [0,1,1,0] なら、盤面を 90 度回転させると [1,0,0,1] に変わり、この sides を持つタイルが jis32 に存在しなければその回転は適用不可

### 接続性の定義

- **接続成立**: 隣接辺が 0 でなく、かつ値が一致  
  例: (E=1, W=1), (N=2, S=2)
- **接続不成立**: 片方が 0、または異なる値  
  例: (E=1, W=2), (E=0, W=0)
- 0-0 の場合は「どちらも接続なし」であり、接続は成立しない

### 単結合性の判定

BFS により「接続成立」エッジだけを使って到達可能な全タイルをカウント
- すべての 32 タイルに到達可能 → 単結合
- 到達不可能なタイルが存在 → 非単結合（複数の島に分かれている）

## ファイル構成

### コア実装
- `explore.py` - メイン列挙プログラム  
  使用法: `python explore.py --rows 4 --cols 8 --tiles jis32.json --max 0`
  - `Solver` クラス: 盤面管理、隣接検査、境界検査、正規化
  - `place_next()`: バックトラッキング探索ルーチン
  - `canonical_signature_checked()`: D4 変換の検証

- `jis32.json` - JIS X 0208 罫線 32 字の完全定義  
  形式: `[{"id":"U250C", "char":"┌", "sides":[0,1,1,0]}, ...]`

- `jis8.json` - テスト用角パーツ 8 字（開発時の検証用）

### ユーティリティ
- `render_solution.py` - 解の検証、ASCII アート生成
  - `verify()`: 隣接・境界条件チェック
  - `render()`: 3×3 ASCII グリッド表現

- `gallery_generator.py` - HTML/テキスト出力生成
  - `is_single_connected()`: BFS で単結合判定
  - 単結合解を先に、非単結合解を後に列挙

### 出力
- `solution_1.json` ～ `solution_37.json` - 代表解（盤面データ）
- `gallery.html` - インタラクティブなHTMLギャラリー
- `solutions_summary.txt` - 全解の ASCII アート一覧

## 今後の拡張予定

### Phase 2a: Code Page 437（40 タイル）

1. **タイル定義**: cp437.json を作成
   - 一重線タイプ（型値 1）と二重線タイプ（型値 2）の 40 字
   - 各タイルの sides を正確に定義

2. **全探索**: 10×4 盤で全探索
   ```
   python explore.py --rows 4 --cols 10 --tiles cp437.json --max 0
   ```

3. **出力**: CP437 用の gallery と summary を生成

### Phase 2b: JIS X 0208（複数盤面サイズ）

1. **9×4 盤**（32 タイル + 4 空白）
   - jis32_with4blanks.json を作成: 空白 = `{"id":"BLANK0", "sides":[0,0,0,0]}` × 4 個
   - 全探索実行

2. **10×4 盤**（32 タイル + 8 空白）
   - jis32_with8blanks.json を作成
   - 全探索実行

### Phase 3: JIS X 0208 大規模盤（9×9 以上、要注意）

⚠️ 盤が大きくなると組み合わせが爆発的に増加するため、単純な全探索は終わらない可能性が高い

**戦略**
1. ブログ記事の制約条件を活用
   - 「外枠の色は不変」など、タイルセット固有の性質
   - 「端のタイルは限られた形のみ可能」など

2. 枝刈り戦略の設計
   - 部分盤面の構造を事前に分析
   - 探索中に条件違反を早期に検出

3. 段階的な実装
   - 9×9 の小規模試行
   - 結果から枝刈り効果を定量化
   - より大きな盤への拡張

## 技術メモ

### 正規化の詳細

```python
# 盤面 B の signature:
# 1. 8 個の D4 変換を生成
# 2. 各変換について:
#    a. 盤面内の各セル (r, c) を変換
#    b. sides も同じ変換を適用: [N,E,S,W] → [N',E',S',W']
#    c. sides_to_id マッピングを確認
#    d. 該当するタイルが存在しない → 変換スキップ
# 3. 有効な変換のみについて型置換 (1↔2) を適用
# 4. すべての有効な署名の辞書順最小値を代表
```

### パフォーマンス特性

- jis32.json 8×4: ~21.8M ノード、数分
- 大きな盤（9×4 以上）: 枝刈りなし は実用的でない
- 枝刈り導入後の性能は未知（Phase 3 で検討）

## 検証と品質保証

✅ **jis8.json 8 タイル**: テスト済み（開発時の早期検出用）
✅ **jis32.json 32 タイル**: 全解が隣接・境界・接続性条件を満たす
✅ **単結合判定**: BFS アルゴリズムで正確に実装
✅ **正規化**: D4 変換時に必ず tiles_to_id 検証

## トラブルシューティング

### Q: タイル個別の回転について
A: 実装しない。タイルは固定向き。盤面変換のみ。

### Q: 大きな盤で遅い場合
A: 枝刈り戦略を導入する。ブログ記事の制約条件を活用。
